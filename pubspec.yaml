name: doumo_test_task
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.5
  dio: ^5.4.3+1
  equatable: ^2.0.5
  flutter_dotenv: ^5.1.0
  fpdart: ^1.1.0
  get_it: ^7.7.0
  json_serializable: ^6.8.0
  json_annotation: ^4.9.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mockito: ^5.4.4
  bloc_test: ^9.1.7
  build_runner: ^2.4.10
  

flutter:
  uses-material-design: true

  assets:
    - .env
